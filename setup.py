from setuptools import setup

setup(name='paypal_transactions_wrapper',
      version='0.1',
      description='A wrapper for Paypal transactions',
      url='#',
      author='Ori-Roza',
      author_email='ori75660@gmail.com',
      license='MIT',
      packages=['paypal_transactions_wrapper'],
      zip_safe=False,
      install_requires=[
            'requests'
      ])
